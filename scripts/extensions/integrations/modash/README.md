# Modash 可选补充层

预留给后续通过已登录 Chrome + Codex 插件执行的 Modash 补充流程。当前不启用 API，不保存登录凭证，也不把批量导出设为默认步骤。

接入时至少记录：候选账号、补充字段、查询时间、缓存有效期、动作类型、操作前后余额和证据截图/来源。输出统一写入 `data/modash/`，再由 SOP V2 适配层合并，不能直接覆盖 Instagram 原始字段。
